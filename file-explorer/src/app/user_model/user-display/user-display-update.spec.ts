import { UserDisplayUpdate } from './user-display-update';

describe('UserDisplayUpdate', () => {
  it('should create an instance', () => {
    expect(new UserDisplayUpdate()).toBeTruthy();
  });
});
